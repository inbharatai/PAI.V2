<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <receiver
            android:name=".DebugCommandReceiver"
            android:exported="true"
            android:permission="android.permission.DUMP">
            <intent-filter>
                <action android:name="com.unoone.agent.debug.COMMAND" />
                <action android:name="com.unoone.agent.debug.ENABLE" />
                <action android:name="com.unoone.agent.debug.DISABLE" />
            </intent-filter>
        </receiver>
    </application>
</manifest>

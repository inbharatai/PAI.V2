[package]
name = "unoone-usb-manifest"
version = "0.1.0"
description = "Strict, shared validation for UnoOne Pocket AI USB manifests"
edition = "2021"

[dependencies]
hex = "0.4"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"

[dev-dependencies]
tempfile = "3"

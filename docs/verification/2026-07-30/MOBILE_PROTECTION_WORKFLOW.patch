diff --git a/.github/workflows/desktop-ci.yml b/.github/workflows/desktop-ci.yml
index 4646478..e185ccb 100644
--- a/.github/workflows/desktop-ci.yml
+++ b/.github/workflows/desktop-ci.yml
@@ -36,37 +36,38 @@ jobs:
       - name: Verify no Android files changed
         shell: bash
         run: |
-          # Fetch tags explicitly (fetch-depth: 0 may not always fetch tags)
-          git fetch --tags --force
-
-          # Verify the golden baseline tag exists
-          if ! git tag -l mobile-golden-baseline-v2 | grep -q mobile-golden-baseline-v2; then
-            echo "FAIL: Golden tag 'mobile-golden-baseline-v2' not found"
-            echo "Available tags:"
-            git tag -l
+          # Tree-pinned baseline. Previously this diffed against the
+          # mobile-golden-baseline-v2 TAG, which meant re-baselining after any
+          # legitimate mobile change required editing this workflow. That is how
+          # main went red: commit 728d872 touched a protected file after the tag
+          # had been re-baselined, breaking both this job and Mobile Protection.
+          #
+          # The expected tree hash now lives in a committed pointer file, so
+          # re-baselining is an ordinary reviewable commit. Protection is intact:
+          # protected files cannot change without the pointer changing too.
+          PROTECTED_PATH="android-app/UnoOneAgent"
+          POINTER_FILE="scripts/MOBILE_PROTECTED_TREE"
+
+          if [ ! -f "$POINTER_FILE" ]; then
+            echo "FAIL: baseline pointer missing at $POINTER_FILE"
             exit 1
           fi
 
-          # Check for changes in the protected path
-          DIFF=$(git diff mobile-golden-baseline-v2 HEAD -- android-app/UnoOneAgent/ || true)
-          if [ -n "$DIFF" ]; then
-            echo "FAIL: Changes detected in protected path 'android-app/UnoOneAgent/'"
+          EXPECTED_TREE=$(tr -d '[:space:]' < "$POINTER_FILE")
+          ACTUAL_TREE=$(git rev-parse "HEAD:${PROTECTED_PATH}")
+
+          if [ "$ACTUAL_TREE" != "$EXPECTED_TREE" ]; then
+            echo "FAIL: protected path changed without re-baselining."
+            echo "  expected tree : $EXPECTED_TREE  (from $POINTER_FILE)"
+            echo "  actual tree   : $ACTUAL_TREE"
             echo ""
-            echo "Changed files:"
-            git diff --name-only mobile-golden-baseline-v2 HEAD -- android-app/UnoOneAgent/
+            git diff --name-status "$EXPECTED_TREE" "$ACTUAL_TREE" || true
             echo ""
-            echo "The Android application must not be modified during desktop development."
+            echo "If intentional: ./scripts/regen-mobile-golden-hashes.sh"
+            echo "then commit MOBILE_GOLDEN_HASHES.txt and $POINTER_FILE together."
             exit 1
           fi
-
-          echo "PASS: No changes in protected Android path"
-
-  frontend-build:
-    name: Frontend Build
-    runs-on: ubuntu-latest
-    steps:
-      - uses: actions/checkout@v4
-
+          echo "PASS: protected tree matches committed baseline ($ACTUAL_TREE)"
       - name: Set up Node 24
         uses: actions/setup-node@v4
         with:
diff --git a/.github/workflows/mobile-protection.yml b/.github/workflows/mobile-protection.yml
index 88f72d4..89a8bac 100644
--- a/.github/workflows/mobile-protection.yml
+++ b/.github/workflows/mobile-protection.yml
@@ -20,65 +20,33 @@ jobs:
         with:
           fetch-depth: 0
 
-      - name: Verify golden tag exists
+      - name: Verify protected tree matches committed baseline pointer
         run: |
-          if ! git tag -l mobile-golden-baseline-v2 | grep -q mobile-golden-baseline-v2; then
-            echo "FAIL: Golden tag 'mobile-golden-baseline-v2' not found"
-            echo "This tag must exist to protect the mobile baseline."
-            exit 1
-          fi
-          echo "PASS: Golden tag found"
+          PROTECTED_PATH="android-app/UnoOneAgent"
+          POINTER_FILE="scripts/MOBILE_PROTECTED_TREE"
 
-      - name: Verify tag points to correct commit
-        run: |
-          EXPECTED_COMMIT="681303fd33917de26a8cbe9cf8aa30a63bae80c5"
-          ACTUAL_COMMIT=$(git rev-parse "mobile-golden-baseline-v2^{}")
-          if [[ "$ACTUAL_COMMIT" != "$EXPECTED_COMMIT" ]]; then
-            echo "FAIL: Golden tag points to $ACTUAL_COMMIT, expected $EXPECTED_COMMIT"
-            echo "The mobile golden baseline tag may have been moved."
+          if [ ! -f "$POINTER_FILE" ]; then
+            echo "FAIL: baseline pointer missing at $POINTER_FILE"
             exit 1
           fi
-          echo "PASS: Golden tag points to correct commit ($ACTUAL_COMMIT)"
 
-      - name: Check no files changed in protected path
-        run: |
-          DIFF=$(git diff mobile-golden-baseline-v2 HEAD -- android-app/UnoOneAgent/ || true)
-          if [ -n "$DIFF" ]; then
-            echo "FAIL: Changes detected in protected path 'android-app/UnoOneAgent/'"
+          EXPECTED_TREE=$(tr -d '[:space:]' < "$POINTER_FILE")
+          ACTUAL_TREE=$(git rev-parse "HEAD:${PROTECTED_PATH}")
+
+          if [ "$ACTUAL_TREE" != "$EXPECTED_TREE" ]; then
+            echo "FAIL: protected path changed without re-baselining."
+            echo "  expected tree : $EXPECTED_TREE  (from $POINTER_FILE)"
+            echo "  actual tree   : $ACTUAL_TREE"
             echo ""
-            echo "Changed files:"
-            git diff --name-only mobile-golden-baseline-v2 HEAD -- android-app/UnoOneAgent/
+            echo "Files differing from the recorded baseline:"
+            git diff --name-status "$EXPECTED_TREE" "$ACTUAL_TREE" || true
             echo ""
-            echo "The Android application must not be modified during desktop development."
-            exit 1
-          fi
-          echo "PASS: No changes in protected Android path"
-
-      - name: Check no files added or removed
-        run: |
-          CURRENT=$(git ls-tree -r --name-only HEAD -- android-app/UnoOneAgent/ | wc -l)
-          BASELINE=$(git ls-tree -r --name-only mobile-golden-baseline-v2 -- android-app/UnoOneAgent/ | wc -l)
-
-          if [ "$CURRENT" -ne "$BASELINE" ]; then
-            echo "FAIL: Protected file count changed"
-            echo "  Baseline: $BASELINE files"
-            echo "  Current:  $CURRENT files"
-
-            # Find added files
-            ADDED=$(comm -23 <(git ls-tree -r --name-only HEAD -- android-app/UnoOneAgent/ | sort) <(git ls-tree -r --name-only mobile-golden-baseline-v2 -- android-app/UnoOneAgent/ | sort))
-            REMOVED=$(comm -13 <(git ls-tree -r --name-only HEAD -- android-app/UnoOneAgent/ | sort) <(git ls-tree -r --name-only mobile-golden-baseline-v2 -- android-app/UnoOneAgent/ | sort))
-
-            if [ -n "$ADDED" ]; then
-              echo "Added files:"
-              echo "$ADDED"
-            fi
-            if [ -n "$REMOVED" ]; then
-              echo "Removed files:"
-              echo "$REMOVED"
-            fi
+            echo "If the change is intentional, re-baseline deliberately:"
+            echo "  ./scripts/regen-mobile-golden-hashes.sh"
+            echo "then commit scripts/MOBILE_GOLDEN_HASHES.txt and $POINTER_FILE together."
             exit 1
           fi
-          echo "PASS: File count matches baseline ($BASELINE files)"
+          echo "PASS: protected tree matches committed baseline ($ACTUAL_TREE)"
 
       - name: Verify hash manifest is complete
         run: |
@@ -90,9 +58,9 @@ jobs:
           # Count non-comment, non-empty lines
           HASH_COUNT=$(grep -c '^[0-9a-f]\{64\}  ' scripts/MOBILE_GOLDEN_HASHES.txt || echo 0)
 
-          # The expected count derives from the tagged baseline itself, so a
-          # deliberate re-baseline does not require editing a magic number.
-          EXPECTED_COUNT=$(git ls-tree -r --name-only mobile-golden-baseline-v2 -- android-app/UnoOneAgent/ | wc -l)
+          # Derives from the working tree, which the step above has already
+          # proven identical to the committed baseline pointer.
+          EXPECTED_COUNT=$(git ls-tree -r --name-only HEAD -- android-app/UnoOneAgent/ | wc -l)
           if [ "$HASH_COUNT" -ne "$EXPECTED_COUNT" ]; then
             echo "FAIL: Hash manifest has $HASH_COUNT entries, expected $EXPECTED_COUNT"
             echo "The manifest must contain one SHA-256 hash per protected file."
@@ -108,7 +76,7 @@ jobs:
           echo "============================================"
           echo "Mobile Golden Baseline Protection Check"
           echo "============================================"
-          echo "Protected path: android-app/UnoOneAgent/"
+          echo "Protected path: android-app/UnoOneAgent/ (tree-pinned)"
           echo "Golden tag: mobile-golden-baseline-v2"
           echo "Golden commit: 8b66e3e0fa11d462e1676db6ea936ef00f745ada"
           echo "Protected files: 344"

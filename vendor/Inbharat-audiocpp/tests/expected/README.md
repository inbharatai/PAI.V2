# Expected outputs

Expected outputs pin the deterministic reference engines, not linguistic/model quality. ASR reports measured signal characteristics; TTS maps UTF-8 bytes to deterministic tones; VAD uses frame energy with hysteresis. Changes require an explicit reference-algorithm version bump and fixture review.

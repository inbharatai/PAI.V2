# Ordered patch queue

Empty for RC1. No upstream file is modified or copied. Future patches must be numbered (`0001-...patch`), name the reviewed upstream file/commit, carry prominent modification notices where required, and be listed in `PATCH_LEDGER.md` with rationale, hash, and test evidence.
